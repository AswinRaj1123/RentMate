import User, { UserRole, IUser } from './user.model';
import Property, { PropertyType, IProperty } from './property.model';
import Lease, { LeaseStatus, ILease } from './lease.model';
import RentShare, { PaymentStatus, IRentShare } from './rentShare.model';

export {
  User,
  UserRole,
  IUser,
  Property,
  PropertyType,
  IProperty,
  Lease,
  LeaseStatus,
  ILease,
  RentShare,
  PaymentStatus,
  IRentShare
};